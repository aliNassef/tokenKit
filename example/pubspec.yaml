name: theme_pkg_example
description: "Demo app for the theme_pkg design-tokens package."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  theme_pkg:
    path: ../
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
